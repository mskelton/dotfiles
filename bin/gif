#!/usr/bin/env bash
# Convert video to gif
# Usage: gif video.mp4 [output.gif]

ffmpeg -i "$1" \
	-vf "fps=10,scale=1080:-1:flags=lanczos,split[s0][s1];[s0]palettegen[p];[s1][p]paletteuse" \
	-loop 0 "${2:-${i%.*}.gif}"
