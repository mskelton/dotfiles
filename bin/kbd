#!/usr/bin/env bash
# Reconnect Advantage 360 keyboard

DEVICE="Adv360-Pro"

blueutil --unpair "$DEVICE"

# Wait for a few seconds
echo 'Waiting for 3 seconds...'
sleep 3

blueutil --pair "$DEVICE"
blueutil --connect "$DEVICE"

echo "Bluetooth device reconnected"
