#!/usr/bin/env bash
# Reload all my custom launchd agents

for path in "$HOME/Library/LaunchAgents/dev.mskelton."*.plist; do
	[[ -e "$path" ]] || continue

	launchctl bootout "gui/$(id -u)" "$path"
	launchctl bootstrap "gui/$(id -u)" "$path"
done
