# Migration Checklist

- Backup pictures and documents
- Backup the following files:
  - `~/.gitconfig`
  - `~/.npmrc`
  - `~/.yarnrc.yml`
  - `~/.zsh_history`
  - `~/.zshcustom`
- Audit dev folder:
  - Run `audit-repos` to check for repos without a remote or with unpushed
    changes
