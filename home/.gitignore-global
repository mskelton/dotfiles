# vim:ft=gitignore

# Why ignore `.git`, isn't it already ignored? Yes it is, but when I run rg or fd
# with a custom ignore setup, I need to make sure `.git` remains excluded.
.git/

# Why mac, just why
.DS_Store

# npm package archives
*.tgz

# Unless I explicitly commit them, I don't want ignore files committed, as they
# are primarily for my setup.
.ignore

# Vim quickfix lists
*.err

# Temporary files
*.tmp
*.swp

# Neovim local config files. I don't want to commit these, as they are specific
# to me. If I want it committed, I'll do so manually.
.nvim.lua
